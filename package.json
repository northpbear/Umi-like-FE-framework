{
  "name": "Umi-like-FE-framework",
  "version": "0.0.1",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "start:app": "pnpm --filter app run serve",
    "build": "pnpm -r --filter ./packages/** run build",
    "dev": "pnpm -r --filter ./packages/* --parallel run dev"
  },
  "devDependencies": {
    "esbuild": "^0.19.10",
    "typescript": "^5.3.3"
  }
}
